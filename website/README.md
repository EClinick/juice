# Juice landing page

The landing page is a Next.js app. From this directory, install dependencies and start the local preview:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Run `npm run build` to verify the production build.
